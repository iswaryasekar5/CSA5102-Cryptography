#include <stdio.h>
#include <string.h>

int main() {
    char p[100];
    int k[100],i,n;

    scanf("%s",p);
    n=strlen(p);

    for(i=0;i<n;i++)
        scanf("%d",&k[i]);

    for(i=0;i<n;i++)
        p[i]=(p[i]-'a'+k[i])%26+'a';

    printf("Cipher: %s",p);
    return 0;
}
