#include <stdio.h>

int main() {
    char s[500];
    int f[26]={0},i;

    scanf("%s",s);

    for(i=0;s[i];i++)
        if(s[i]>='A'&&s[i]<='Z')
            f[s[i]-'A']++;

    for(i=0;i<26;i++)
        if(f[i])
            printf("%c = %d\n",'A'+i,f[i]);

    return 0;
}
