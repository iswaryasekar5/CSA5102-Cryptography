#include<stdio.h>
#include<string.h>
int main(){
    char p[100]; int k[100],i,n;
    gets(p);
    n=strlen(p);
    for(i=0;i<n;i++) scanf("%d",&k[i]);
    for(i=0;i<n;i++)
        if(p[i]>='a'&&p[i]<='z')
            p[i]=((p[i]-'a'+k[i])%26)+'a';
    printf("%s",p);
}
